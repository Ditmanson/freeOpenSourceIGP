# 06: Update navigation and clean up old folders

**What to build:** `hugo.yaml`'s main menu reflects the new section structure, every old now-empty folder is deleted, and the whole site is verified to build cleanly end-to-end.

**Blocked by:** 02, 03, 04, 05 (all content must be migrated before the old folders can be safely deleted and the menu repointed)

**Status:** done (commit e1f91e9)

- [ ] `hugo.yaml` menu entries for `BH`, `IGP`, `Obedience`, `Nosework`, `Protection`, `Schedule` are removed
- [ ] `hugo.yaml` menu gains four entries in this order: `Dogs` (`/dogs`), `Meetups` (`/meetups`), `Tech` (`/tech`), `Misc` (`/misc`), followed by the existing `Tags` (`/tags/`) entry, with ascending `weight` values preserving that order
- [ ] `taxonomies: tag: "tags"` in `hugo.yaml` is unchanged
- [ ] Old folders `content/BH/`, `content/IGP/`, `content/Obedience/`, `content/Nosework/`, `content/Protection/`, `content/Schedule/`, `content/Misc/` (capitalized) no longer exist, and are confirmed empty of unmigrated content before deletion
- [ ] `content/_index.md` (homepage) is untouched
- [ ] A full `hugo` build (matching whatever the deploy step normally uses, e.g. `hugo --gc --minify` if that's the existing convention) completes with no errors or warnings about missing templates/broken shortcodes
- [ ] Section pages `/dogs/`, `/meetups/`, `/tech/`, `/misc/`, `/tags/` are all reachable and list the expected posts
- [ ] A spot-check of at least one migrated post per section renders correctly with original content intact
- [ ] `public/` is not modified by this ticket — it remains whatever it was before, to be regenerated by the normal deploy step separately
